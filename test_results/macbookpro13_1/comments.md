# Notes

## Desktop

The testing device has hardware issue with display ("apple flexgate"). I didn't try configuring any desktop environment on the laptop.

## Wi-Fi

The `if_brcmfmac.ko` kernel module is a custom driver for Broadcom BCM4350 Wi-Fi chip (`brcmfmac0@pci0:2:0:0`).
